#include <stdio.h>
#include <stdlib.h>
#include<graphics.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() {
	int gd=DETECT,gm;
     initgraph(&gd,&gm,)
	circle(200,124,50);
	getch();
}
